# bootstrap.sh

## `bootstrap.sh` is for the bootstrapping of the linux system.

```bash <(curl -fsSL https://tinyurl.com/bootstraplinux)```

or

```bash <(curl -fsSL https://tinyurl.com/bootstraplinux)```
